# ================================================================
#  THE INNER CITADEL — Python Dependencies
#  Python 3.13.0 | Windows | CPU-Only | Intel | No GPU
#  All versions verified compatible with Python 3.13 on Windows
#  Research date: May 2026
# ================================================================

# ── Web Framework ────────────────────────────────────────────────
fastapi==0.136.3
uvicorn[standard]==0.48.0
python-multipart==0.0.29
python-dotenv==1.1.0

# ── LLM APIs ─────────────────────────────────────────────────────
# IMPORTANT: google-generativeai reached EOL Nov 30, 2025
# Replaced by google-genai (different import: from google import genai)
google-genai==2.6.0
httpx==0.28.1
groq==1.2.0

# ── Text-to-Speech — Microsoft Neural TTS ────────────────────────
# edge-tts replaces kokoro (kokoro incompatible with Python 3.13)
# Free, no API key, no system deps, no GPU needed
# Requires internet (same as Groq/Pexels API calls)
edge-tts==7.2.8

# ── Audio Processing ─────────────────────────────────────────────
soundfile==0.13.1

# ── Video Composition — MoviePy 2.x ──────────────────────────────
# moviepy 2.x: use `from moviepy import X` (not moviepy.editor)
# FFmpeg auto-downloaded on first use via imageio-ffmpeg
moviepy==2.2.1
imageio>=2.36.0
imageio-ffmpeg>=0.5.1

# ── Image / Numerical ────────────────────────────────────────────
Pillow==12.2.0
numpy>=2.1.0

# ── Subtitle Generation ──────────────────────────────────────────
pysubs2==1.8.1

# ── HTTP / Retry ─────────────────────────────────────────────────
requests==2.34.2
tenacity==9.1.4

# ── Semantic Similarity ──────────────────────────────────────────
# NOTE: PyTorch CPU is installed separately in start_dev.bat FIRST
# to prevent GPU version being pulled in accidentally
faiss-cpu==1.14.2
sentence-transformers==5.5.1

# ── Data Validation ──────────────────────────────────────────────
pydantic==2.13.4

# ── Logging ──────────────────────────────────────────────────────
loguru==0.7.3

# ── Ngrok Tunnel (exposes localhost:8000 to Vercel) ───────────────
pyngrok==8.1.2

# ── Async / File IO ──────────────────────────────────────────────
aiofiles
anyio
