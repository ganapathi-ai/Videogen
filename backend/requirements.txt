# ================================================================
#  THE INNER CITADEL — Python Dependencies
#  CPU-Only Mode (Intel, No NVIDIA GPU)
#  30GB RAM — all models fit comfortably
# ================================================================

# ── Web Framework (FastAPI — no Celery/Redis needed) ────────────
fastapi==0.115.5
uvicorn[standard]==0.32.0
python-multipart==0.0.12
python-dotenv==1.0.1

# ── LLM — Multi-provider (Groq + OpenRouter + Gemini) ──────────
google-generativeai
httpx==0.27.0
groq

# ── Text-to-Speech — Kokoro (CPU Mode, ~300MB model) ───────────
# kokoro 0.7.x = compatible with Python 3.13
# kokoro 0.8+ requires Python <3.13 (not available for Python 3.13)
kokoro==0.7.16
soundfile==0.12.1
espeakng
sphinxbase

# ── Speech Alignment — WhisperX (CPU, int8 quantization) ───────
# whisperx is optional — fallback timing used if not installed
# Install separately (needs torch first):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install whisperx
# Listed here as optional:
# whisperx

# ── PyTorch CPU (MUST install before whisperx) ──────────────────
# Run this FIRST if you want WhisperX alignment:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
# Otherwise the proportional timing fallback handles it automatically.

# ── Video Composition ────────────────────────────────────────────
moviepy==1.0.3
Pillow>=10.4.0
numpy>=1.26.4

# ── Subtitle Generation ──────────────────────────────────────────
pysubs2==1.7.3

# ── Media / HTTP ─────────────────────────────────────────────────
requests==2.32.3
tenacity==8.2.3

# ── Semantic Similarity (FAISS CPU version) ──────────────────────
faiss-cpu
sentence-transformers

# ── Pydantic + Validation ────────────────────────────────────────
pydantic==2.10.4

# ── Logging ──────────────────────────────────────────────────────
loguru==0.7.2

# ── Ngrok Tunnel (exposes local backend to Vercel) ───────────────
pyngrok==7.2.0

# ── Utilities ─────────────────────────────────────────────────────
pathlib2==2.3.7
