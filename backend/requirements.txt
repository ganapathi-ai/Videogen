# ================================================================
#  THE INNER CITADEL — Python Dependencies
#  Python 3.13.0 | Windows | CPU-Only | Intel | No GPU
#  ALL versions verified compatible with Python 3.13 on Windows
# ================================================================

# ── Web Framework ────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.0
python-multipart==0.0.12
python-dotenv==1.0.1

# ── LLM — Groq + OpenRouter + Gemini ────────────────────────────
google-generativeai
httpx==0.27.0
groq

# ── Text-to-Speech — edge-tts (Microsoft Neural TTS) ────────────
# edge-tts works on Python 3.13, no system deps, no GPU needed
# Uses Microsoft Edge's neural voices — very high quality
# Requires internet connection (same as Groq/Pexels API calls)
edge-tts

# ── Audio processing ─────────────────────────────────────────────
soundfile==0.12.1
scipy

# ── Video Composition — MoviePy 2.x for Python 3.13 ─────────────
# moviepy 2.x is the Python 3.13 compatible version
moviepy==2.2.1
Pillow>=10.4.0
numpy>=1.26.4
imageio>=2.36.0
imageio-ffmpeg>=0.5.1

# ── Subtitle Generation ──────────────────────────────────────────
pysubs2==1.7.3

# ── Media / HTTP ─────────────────────────────────────────────────
requests==2.32.3
tenacity==8.2.3
aiofiles

# ── Semantic Similarity ──────────────────────────────────────────
# faiss-cpu: no GPU needed, CPU only
faiss-cpu
sentence-transformers

# ── Pydantic + Validation ────────────────────────────────────────
pydantic==2.10.4

# ── Logging ──────────────────────────────────────────────────────
loguru==0.7.2

# ── Ngrok Tunnel (exposes local backend to Vercel) ───────────────
pyngrok==7.2.0

# ── Async support ────────────────────────────────────────────────
anyio
